# INITIAL MERGE RECONCILIATION — CURSOR

Antigravity's V5 foundation branch has been merged to `main`.

1. Fetch and integrate latest `origin/main` into `agent/cursor`.
2. Inspect Antigravity's canonical registry/taxonomy/ontology/checkpoint changes and its V5 foundation handoff.
3. Confirm your deterministic contracts/templates/validator still represent the now-canonical lifecycle without contradiction.
4. Make only compatibility fixes that are actually required.
5. Rerun Foundry validator, Foundry unit tests and baseline validator.
6. Update `foundry/handoffs/CURSOR_V5_FOUNDATION.md` with reconciliation result.
7. Commit/push.
8. End exactly `READY_FOR_REVIEW` only when all validation passes.
