# NORMAL CYCLE — NEW WORKFLOW / SKILL FAMILY

Apply this to the recurring activity/need supplied by the human in the same message or immediately after this prompt.

1. Sync `agent/antigravity` from latest `origin/main`.
2. Create one stable `candidate_id` and its folder under `foundry/candidates/`.
3. Run G0→G5 from `SF-WF-001_SKILL_GENESIS` autonomously.
4. Reconstruct the real process before designing Skills.
5. Mine capabilities; do not map every workflow step to a Skill.
6. Compare every capability against the registry with mandatory precedence `REUSE → EXTEND → MODE → DEPENDENT_SKILL → NEW_SKILL → NO_SKILL`.
7. Research only when it materially improves the architecture decision; record skip otherwise.
8. Produce the smallest coherent G5 architecture/closure proposal.
9. Set candidate state `AWAITING_HUMAN_DECISION`, update checkpoint/handoff, commit/push.
10. Stop at G6 and present the proposal clearly for the human's decision.

Ask for clarification only if indispensable missing process information would materially change the architecture. Do not build or self-approve.
