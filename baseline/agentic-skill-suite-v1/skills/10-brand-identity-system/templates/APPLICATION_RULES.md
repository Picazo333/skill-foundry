# Application Rules — [Brand Name]

Applies the tokens/rules in `IDENTITY_SYSTEM.md` / `DESIGN_TOKENS.json`
across production surfaces. Minimum five surfaces unless `constraints`
recorded an explicit channel limitation (state it here if so).

## Web
- Tokens used unchanged: [...]
- Adaptations: [breakpoint-specific type/spacing steps, header treatment]
- Example: [one concrete application]

## Social
- Tokens used unchanged: [...]
- Adaptations: safe zones and crop ratios per placement (square / vertical
  / story) [...]; minimum logo size at each placement (must not go below
  `logo.minSize.digital`)
- Example: [one concrete application]

## Decks / documents
- Tokens used unchanged: [...]
- Adaptations: slide margins, maximum type step per slide role (title vs.
  body), table/chart color mapping to `color.semantic.*`
- Example: [one concrete application]

## Ads
- Tokens used unchanged: [...]
- Adaptations: legal-line placement and minimum size, safe-zone per format,
  motion restraint rule if animated units are in scope
- Example: [one concrete application]

## Product / UI
- Tokens used unchanged: [...]
- Adaptations: component-level token usage (buttons, inputs, states),
  focus-indicator contrast (must clear 3:1 per
  `references/contrast-verification-method.md`), dark-mode mapping if in
  scope
- Example: [one concrete application]

## Channel limitation (if applicable)
[State explicitly if `constraints` limited coverage to fewer than five
surfaces, and which ones were excluded and why — never silently omit.]
