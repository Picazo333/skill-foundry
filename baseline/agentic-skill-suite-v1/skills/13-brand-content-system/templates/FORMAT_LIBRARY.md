# Format Library — [Brand]

> One format archetype per pillar/channel pair. A format must be a
> repeatable structure, not a one-off idea (SKILL.md step 4).

## Format: [Name] — Pillar: [pillar name]
- **Hook:** [pattern, not a specific line — e.g. "open on the objection the
  audience already has, stated in their words"]
- **Body structure:** [the repeatable shape — e.g. claim → mechanism → proof]
- **Proof element:** [what specific, brand-owned proof slots in every time —
  data point, customer case, founder demonstration, etc.]
- **CTA:** [the repeatable ask for this format]
- **Target duration/length:** [e.g. 30–45s video / 150–200 word caption]

### Channel variants
| Channel | What changes from the base format | Why (native constraint) |
|---|---|---|
| | | |
| | | |

<!-- Repeat per pillar. A pillar may have more than one format archetype
     (e.g. a short-form video format and a long-form written format), but
     each must independently satisfy the repeatability test. -->
