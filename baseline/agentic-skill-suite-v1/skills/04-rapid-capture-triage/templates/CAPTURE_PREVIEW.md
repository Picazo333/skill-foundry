# Capture Preview

> Generated by `rapid-capture-triage`. Every line below traces to a verbatim
> fragment from the raw capture. Nothing here reflects a guessed priority,
> owner, or category unless the capture stated or clearly implied it.
> Items that could not be resolved this cleanly are in `AMBIGUITIES.md`, not
> here.

**Source capture:** <path/description of the raw capture>
**Run date:** <date>
**Mode:** QUICK | STANDARD | UPDATE

## New projects
- **<project name>** — <one-line description> — source: "<verbatim fragment>"

## New processes
- **<process name>** — <one-line description> — source: "<verbatim fragment>"

## Tasks
- **<task name>** (project: <parent project, or "unassigned">) — <notes> —
  priority/time signal: <explicit signal or "none stated, capture order N">
  — source: "<verbatim fragment>"

## Recurrent items
- **<recurrent item name>** — cadence: <explicit cadence, e.g. "weekly"> —
  source: "<verbatim fragment>"

## Supervision items
- **<supervision item name>** — ongoing oversight, no fixed completion —
  source: "<verbatim fragment>"

## Doubts
- **<doubt>** — flagged, not resolved into a decision — source: "<verbatim fragment>"

## Ideas
- **<idea>** — source: "<verbatim fragment>"

## Backlog
- **<backlog item>** — source: "<verbatim fragment>"

## Renames
- **<old name> → <new name>** — matched existing item <id/name> — source: "<verbatim fragment>"

## Moves
- **<item>**: <old parent> → <new parent> — source: "<verbatim fragment>"

## Removals
- **<item>** — explicit removal — source: "<verbatim fragment>"

## Preserved (explicitly confirmed unchanged)
- **<item>** — capture confirmed no change — source: "<verbatim fragment>"

---
Unresolved items from this capture are listed in `AMBIGUITIES.md`, not
above. If a section has no entries, omit it rather than leaving it empty —
an omitted section is not the same as "nothing happened here", it means the
capture said nothing about that category.
