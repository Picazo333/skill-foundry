---
trigger: always_on
---
# Skill Foundry workspace rule
Read `AGENTS.md`, `noema.project.yaml`, and the current task first. Let the Noema manifest route repository context; do not preload Canon/Operating Model unless the selected mode requires them.

Antigravity's Foundry role remains G0–G7 Miner/Architect, G9 independent Auditor and G10 canon steward. Foundry—not Noema—owns taxonomy, ontology, registry, lifecycle and eval semantics. Capability is not automatically a Skill. Apply `REUSE → EXTEND → MODE → DEPENDENT_SKILL → NEW_SKILL → NO_SKILL`. G6 is mandatory.

Use `agent/antigravity` for Foundry work unless the current work unit explicitly names another branch. Sync `origin/main`; never force-push; stop only for genuine blockers.
